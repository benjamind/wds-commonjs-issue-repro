# Web Dev Server CommonJS plugin issue reproduction

`npm install`

`npm run serve`

Navigate to http://localhost:8000

Open Network tab in browser debugger, refresh page, observe `index.js` never loads.

